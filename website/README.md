# website

```bash
npm install
```

Copy `.env.example` to `.env` and provide a `DATABASE_URL` with your connection string.

```bash
npm run db:migrate
```

```bash
npm run dev
```

`http://localhost:5173`.
